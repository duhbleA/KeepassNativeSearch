<UserControl x:Class="KeepassNativeSearch.PreviewPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:res="clr-namespace:KeepassNativeSearch"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="350">
    <Grid Margin="5, 5, 5, 5">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition Height="3*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition MinWidth="85" />
            <ColumnDefinition Width="4*" />
        </Grid.ColumnDefinitions>
        <Label Margin="0,0,0, 0" Grid.Row="0" Grid.Column="0" FontWeight="Bold" Content="{x:Static res:Resources.TitleLabel}"/>
        <Label Margin="0,0,0,0" x:Name="TitleLabel" Grid.Row="0" Grid.Column="1" />
        
        <Label Margin="0,0,0, 0" Grid.Row="1" Grid.Column="0" FontWeight="Bold" Content="{x:Static res:Resources.GroupLabel}"/>
        <Label Margin="0,0,0,0" x:Name="GroupLabel" Grid.Row="1" Grid.Column="1" />

        <Label Margin="0,0,0,0" Grid.Row="2" Grid.Column="0" FontWeight="Bold" Content="{x:Static res:Resources.UsernameLabel}"/>
        <Label Margin="0,0,0,0" Grid.Row="2" Grid.Column="1" x:Name="UsernameLabel" />

        <Label Grid.Row="3" Grid.Column="0" Margin="0,0,0,0" MouseLeftButtonDown ="PasswordLabelOnMouseLeftButtonDown" FontWeight="Bold" Content="{x:Static res:Resources.PasswordShowHideLabel}"/>
        <Label Grid.Row="3" Grid.Column="1" Margin="5,0,0,0" x:Name="PasswordLabel" FontFamily="Consolas"/>

        <Label Margin="0,0,0,0" FontWeight="Bold" Grid.Row="4" Grid.Column="0" Content="{x:Static res:Resources.UrlLabel}"/>
        <Label Margin="0,0,0,0" x:Name="UrlLabel" Grid.Row="4" Grid.Column="1" />
        <Label Margin="0,0,0,0" FontWeight="Bold" Grid.Row="5" Grid.Column="0" Content="{x:Static res:Resources.TagsLabel}" />
        <Label Margin="0,0,0,0" x:Name="TagsLabel" Grid.Row="5" Grid.Column="1" />
        <Label Margin="0,0,0,0" FontWeight="Bold" Grid.Row="6" Grid.Column="0" Content="{x:Static res:Resources.NoteLabel}"/>
        <TextBox x:Name="NotesTextBlock"
                 Grid.Row="6" Grid.Column="1"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Disabled" />
    </Grid>
</UserControl>